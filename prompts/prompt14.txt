Please move the RealtimeSession out of the simple_rtc crate and move it into a completely new realtime crate.
backend/src/main.rs should be responsible for spawning the TCP server accepting all the realtime sessions.
The realtime sessions should be accepted from a different port, let's say 9002 -
so that means we can have a completely different TCP server socket.
Furthermore, we should have a RealtimeSessionsManager that manages all the realtime sessions (and guards with a mutex).
It will map clientId -> RealtimeSesssion and provide a `get()` function that the
simple_rtc crate can then use to look up the RealtimeSession for a given clientId.

-------------------------------------------------------

In src/main.rs , can we not create `let realtime_sessions = RealtimeSessionsManager::default();`
and instead let the RealtimeServer own it?

